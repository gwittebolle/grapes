<div class="container">
<div>
<%= link_to "Ajouter une offre", new_wine_path, class:"btn btn-dark" %>
</div>
  <div class="row justify-content-between ">
      <% @wines.each do |wine| %>
            <%= wine.name %>
            <%= wine.price %>
            <%= wine.name %>
            <%= wine.region %>
            <%= wine.domain %>
            <%= wine.description %>
            <%= wine.year %>
            <%= wine.price %>
            <%= wine.stock_quantity %>
            <%= wine.category %>
            <%= wine.grape_variety %>
            <%= wine.alcohol_level %>
            <%= link_to "En savoir plus", wine_path(wine), class:"btn btn-dark" %>
      <% end %>
  </div>
</div>
